window.addEventListener('load', () => {
  // Плавное появление
  document.querySelector('.content').classList.add('show');

  // Показ разрешения (временно — но убрано по твоему запросу)
  // const resolution = `${window.innerWidth} x ${window.innerHeight}`;
  // document.getElementById('resolution').textContent = `Разрешение: ${resolution}`;
});
